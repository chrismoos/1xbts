[package]
name = "limesuite-sys"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Low-level bindings to the LimeSuite library for LimeSDR devices"
links = "LimeSuite"
build = "build.rs"

[lib]
doctest = false

[dependencies]

[build-dependencies]
pkg-config = "0.3"
bindgen = "0.72"

[lints]
workspace = true
