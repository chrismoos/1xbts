[package]
name = "cdma-a11"
version = "0.1.0"
edition = "2024"
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "A11 packet-data signaling support for the 1xBTS packet core."

[dependencies]
md-5 = "0.10"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["net"] }

[lib]
path = "src/lib.rs"

[lints]
workspace = true
