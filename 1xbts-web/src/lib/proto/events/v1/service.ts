// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v6.33.4
// source: events/v1/service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import type { CallContext, CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../google/protobuf/timestamp";
import { AnNetworkEvent } from "./an";
import { BscNetworkEvent } from "./bsc";
import { BtsNetworkEvent } from "./bts";
import { HlrNetworkEvent } from "./hlr";
import { MscNetworkEvent } from "./msc";
import { PcfNetworkEvent } from "./pcf";
import { PdsnNetworkEvent } from "./pdsn";
import { SmscNetworkEvent } from "./smsc";

export const protobufPackage = "events.v1";

/** Originating component identity for a NetworkEvent. */
export enum EventSource {
  EVENT_SOURCE_UNSPECIFIED = 0,
  EVENT_SOURCE_BTS = 1,
  EVENT_SOURCE_BSC = 2,
  EVENT_SOURCE_MSC = 3,
  EVENT_SOURCE_PCF = 4,
  EVENT_SOURCE_PDSN = 5,
  EVENT_SOURCE_HLR = 6,
  EVENT_SOURCE_SMSC = 7,
  EVENT_SOURCE_NIB = 8,
  EVENT_SOURCE_AN = 9,
  UNRECOGNIZED = -1,
}

export function eventSourceFromJSON(object: any): EventSource {
  switch (object) {
    case 0:
    case "EVENT_SOURCE_UNSPECIFIED":
      return EventSource.EVENT_SOURCE_UNSPECIFIED;
    case 1:
    case "EVENT_SOURCE_BTS":
      return EventSource.EVENT_SOURCE_BTS;
    case 2:
    case "EVENT_SOURCE_BSC":
      return EventSource.EVENT_SOURCE_BSC;
    case 3:
    case "EVENT_SOURCE_MSC":
      return EventSource.EVENT_SOURCE_MSC;
    case 4:
    case "EVENT_SOURCE_PCF":
      return EventSource.EVENT_SOURCE_PCF;
    case 5:
    case "EVENT_SOURCE_PDSN":
      return EventSource.EVENT_SOURCE_PDSN;
    case 6:
    case "EVENT_SOURCE_HLR":
      return EventSource.EVENT_SOURCE_HLR;
    case 7:
    case "EVENT_SOURCE_SMSC":
      return EventSource.EVENT_SOURCE_SMSC;
    case 8:
    case "EVENT_SOURCE_NIB":
      return EventSource.EVENT_SOURCE_NIB;
    case 9:
    case "EVENT_SOURCE_AN":
      return EventSource.EVENT_SOURCE_AN;
    case -1:
    case "UNRECOGNIZED":
    default:
      return EventSource.UNRECOGNIZED;
  }
}

export function eventSourceToJSON(object: EventSource): string {
  switch (object) {
    case EventSource.EVENT_SOURCE_UNSPECIFIED:
      return "EVENT_SOURCE_UNSPECIFIED";
    case EventSource.EVENT_SOURCE_BTS:
      return "EVENT_SOURCE_BTS";
    case EventSource.EVENT_SOURCE_BSC:
      return "EVENT_SOURCE_BSC";
    case EventSource.EVENT_SOURCE_MSC:
      return "EVENT_SOURCE_MSC";
    case EventSource.EVENT_SOURCE_PCF:
      return "EVENT_SOURCE_PCF";
    case EventSource.EVENT_SOURCE_PDSN:
      return "EVENT_SOURCE_PDSN";
    case EventSource.EVENT_SOURCE_HLR:
      return "EVENT_SOURCE_HLR";
    case EventSource.EVENT_SOURCE_SMSC:
      return "EVENT_SOURCE_SMSC";
    case EventSource.EVENT_SOURCE_NIB:
      return "EVENT_SOURCE_NIB";
    case EventSource.EVENT_SOURCE_AN:
      return "EVENT_SOURCE_AN";
    case EventSource.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Account lifecycle state. Mirrors `cdma_hlr::model::SubscriberStatus`. */
export enum SubscriberStatus {
  SUBSCRIBER_STATUS_UNSPECIFIED = 0,
  SUBSCRIBER_STATUS_ACTIVE = 1,
  SUBSCRIBER_STATUS_SUSPENDED = 2,
  SUBSCRIBER_STATUS_DISABLED = 3,
  UNRECOGNIZED = -1,
}

export function subscriberStatusFromJSON(object: any): SubscriberStatus {
  switch (object) {
    case 0:
    case "SUBSCRIBER_STATUS_UNSPECIFIED":
      return SubscriberStatus.SUBSCRIBER_STATUS_UNSPECIFIED;
    case 1:
    case "SUBSCRIBER_STATUS_ACTIVE":
      return SubscriberStatus.SUBSCRIBER_STATUS_ACTIVE;
    case 2:
    case "SUBSCRIBER_STATUS_SUSPENDED":
      return SubscriberStatus.SUBSCRIBER_STATUS_SUSPENDED;
    case 3:
    case "SUBSCRIBER_STATUS_DISABLED":
      return SubscriberStatus.SUBSCRIBER_STATUS_DISABLED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SubscriberStatus.UNRECOGNIZED;
  }
}

export function subscriberStatusToJSON(object: SubscriberStatus): string {
  switch (object) {
    case SubscriberStatus.SUBSCRIBER_STATUS_UNSPECIFIED:
      return "SUBSCRIBER_STATUS_UNSPECIFIED";
    case SubscriberStatus.SUBSCRIBER_STATUS_ACTIVE:
      return "SUBSCRIBER_STATUS_ACTIVE";
    case SubscriberStatus.SUBSCRIBER_STATUS_SUSPENDED:
      return "SUBSCRIBER_STATUS_SUSPENDED";
    case SubscriberStatus.SUBSCRIBER_STATUS_DISABLED:
      return "SUBSCRIBER_STATUS_DISABLED";
    case SubscriberStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Radio-layer identifiers known at the producer site. Producers fill in
 * whichever fields they have; the bus uses these to resolve an HLR
 * `Subscriber` record before fan-out. Empty fields mean unknown — not
 * the same as a particular zero/empty value.
 */
export interface MobileIdentity {
  /** IMSI as 10-15 decimal digits. Empty if unknown. */
  imsi: string;
  /** Raw 32-bit ESN value. Zero if unknown (treated as "not set"). */
  esn: number;
  /** MEID as a 14-hex string. Empty if unknown. */
  meid: string;
}

/**
 * HLR-resolved subscriber record. Populated by the bus when an HLR
 * lookup of `MobileIdentity` succeeds. Empty for roaming or unprovisioned
 * mobiles (the air-interface event still flows with `identity` set).
 */
export interface Subscriber {
  /** HLR record UUID — stable correlation key for this subscriber. */
  subscriberId: string;
  /** MDN / phone number in E.164 form. */
  phoneNumber: string;
  /** Human-readable name from the HLR record. */
  displayName: string;
  /** Account lifecycle state from the HLR record. */
  status: SubscriberStatus;
}

/**
 * One event on the unified bus. The top-level envelope is intentionally
 * thin — each component owns its own wrapper sub-message in a dedicated
 * proto file. Adding a new component is one new file + one new oneof arm.
 */
export interface NetworkEvent {
  timestamp: Date | undefined;
  source: EventSource;
  /**
   * Monotonic per-server sequence; the bus assigns this before fan-out.
   * Producers leave it zero on Publish.
   */
  sequence: string;
  /** Free-form producer identity (e.g. "pdsn-0") for multi-instance setups. */
  producerInstance: string;
  /**
   * The mobile this event pertains to. Either or both of `identity` and
   * `subscriber` may be set by the producer; the bus enriches the
   * missing side via HLR. Both empty = system event with no associated
   * mobile.
   */
  identity: MobileIdentity | undefined;
  subscriber: Subscriber | undefined;
  pdsn?: PdsnNetworkEvent | undefined;
  bsc?: BscNetworkEvent | undefined;
  msc?: MscNetworkEvent | undefined;
  pcf?: PcfNetworkEvent | undefined;
  hlr?: HlrNetworkEvent | undefined;
  smsc?: SmscNetworkEvent | undefined;
  bts?: BtsNetworkEvent | undefined;
  an?: AnNetworkEvent | undefined;
}

export interface PublishRequest {
  event: NetworkEvent | undefined;
}

export interface PublishAck {
  assignedSequence: string;
}

export interface ListenEventsRequest {
  /** Empty = all sources. */
  sourceFilter: EventSource[];
}

function createBaseMobileIdentity(): MobileIdentity {
  return { imsi: "", esn: 0, meid: "" };
}

export const MobileIdentity: MessageFns<MobileIdentity> = {
  encode(message: MobileIdentity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.imsi !== "") {
      writer.uint32(10).string(message.imsi);
    }
    if (message.esn !== 0) {
      writer.uint32(16).uint32(message.esn);
    }
    if (message.meid !== "") {
      writer.uint32(26).string(message.meid);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MobileIdentity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMobileIdentity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.imsi = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.esn = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.meid = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MobileIdentity {
    return {
      imsi: isSet(object.imsi) ? globalThis.String(object.imsi) : "",
      esn: isSet(object.esn) ? globalThis.Number(object.esn) : 0,
      meid: isSet(object.meid) ? globalThis.String(object.meid) : "",
    };
  },

  toJSON(message: MobileIdentity): unknown {
    const obj: any = {};
    if (message.imsi !== "") {
      obj.imsi = message.imsi;
    }
    if (message.esn !== 0) {
      obj.esn = Math.round(message.esn);
    }
    if (message.meid !== "") {
      obj.meid = message.meid;
    }
    return obj;
  },

  create(base?: DeepPartial<MobileIdentity>): MobileIdentity {
    return MobileIdentity.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MobileIdentity>): MobileIdentity {
    const message = createBaseMobileIdentity();
    message.imsi = object.imsi ?? "";
    message.esn = object.esn ?? 0;
    message.meid = object.meid ?? "";
    return message;
  },
};

function createBaseSubscriber(): Subscriber {
  return { subscriberId: "", phoneNumber: "", displayName: "", status: 0 };
}

export const Subscriber: MessageFns<Subscriber> = {
  encode(message: Subscriber, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.subscriberId !== "") {
      writer.uint32(10).string(message.subscriberId);
    }
    if (message.phoneNumber !== "") {
      writer.uint32(18).string(message.phoneNumber);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Subscriber {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSubscriber();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.subscriberId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.phoneNumber = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Subscriber {
    return {
      subscriberId: isSet(object.subscriberId)
        ? globalThis.String(object.subscriberId)
        : isSet(object.subscriber_id)
        ? globalThis.String(object.subscriber_id)
        : "",
      phoneNumber: isSet(object.phoneNumber)
        ? globalThis.String(object.phoneNumber)
        : isSet(object.phone_number)
        ? globalThis.String(object.phone_number)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      status: isSet(object.status) ? subscriberStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: Subscriber): unknown {
    const obj: any = {};
    if (message.subscriberId !== "") {
      obj.subscriberId = message.subscriberId;
    }
    if (message.phoneNumber !== "") {
      obj.phoneNumber = message.phoneNumber;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.status !== 0) {
      obj.status = subscriberStatusToJSON(message.status);
    }
    return obj;
  },

  create(base?: DeepPartial<Subscriber>): Subscriber {
    return Subscriber.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Subscriber>): Subscriber {
    const message = createBaseSubscriber();
    message.subscriberId = object.subscriberId ?? "";
    message.phoneNumber = object.phoneNumber ?? "";
    message.displayName = object.displayName ?? "";
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseNetworkEvent(): NetworkEvent {
  return {
    timestamp: undefined,
    source: 0,
    sequence: "0",
    producerInstance: "",
    identity: undefined,
    subscriber: undefined,
    pdsn: undefined,
    bsc: undefined,
    msc: undefined,
    pcf: undefined,
    hlr: undefined,
    smsc: undefined,
    bts: undefined,
    an: undefined,
  };
}

export const NetworkEvent: MessageFns<NetworkEvent> = {
  encode(message: NetworkEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(10).fork()).join();
    }
    if (message.source !== 0) {
      writer.uint32(16).int32(message.source);
    }
    if (message.sequence !== "0") {
      writer.uint32(24).uint64(message.sequence);
    }
    if (message.producerInstance !== "") {
      writer.uint32(34).string(message.producerInstance);
    }
    if (message.identity !== undefined) {
      MobileIdentity.encode(message.identity, writer.uint32(42).fork()).join();
    }
    if (message.subscriber !== undefined) {
      Subscriber.encode(message.subscriber, writer.uint32(50).fork()).join();
    }
    if (message.pdsn !== undefined) {
      PdsnNetworkEvent.encode(message.pdsn, writer.uint32(82).fork()).join();
    }
    if (message.bsc !== undefined) {
      BscNetworkEvent.encode(message.bsc, writer.uint32(90).fork()).join();
    }
    if (message.msc !== undefined) {
      MscNetworkEvent.encode(message.msc, writer.uint32(98).fork()).join();
    }
    if (message.pcf !== undefined) {
      PcfNetworkEvent.encode(message.pcf, writer.uint32(106).fork()).join();
    }
    if (message.hlr !== undefined) {
      HlrNetworkEvent.encode(message.hlr, writer.uint32(114).fork()).join();
    }
    if (message.smsc !== undefined) {
      SmscNetworkEvent.encode(message.smsc, writer.uint32(122).fork()).join();
    }
    if (message.bts !== undefined) {
      BtsNetworkEvent.encode(message.bts, writer.uint32(130).fork()).join();
    }
    if (message.an !== undefined) {
      AnNetworkEvent.encode(message.an, writer.uint32(138).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NetworkEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNetworkEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sequence = reader.uint64().toString();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.producerInstance = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.identity = MobileIdentity.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.subscriber = Subscriber.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.pdsn = PdsnNetworkEvent.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.bsc = BscNetworkEvent.decode(reader, reader.uint32());
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.msc = MscNetworkEvent.decode(reader, reader.uint32());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.pcf = PcfNetworkEvent.decode(reader, reader.uint32());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.hlr = HlrNetworkEvent.decode(reader, reader.uint32());
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.smsc = SmscNetworkEvent.decode(reader, reader.uint32());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.bts = BtsNetworkEvent.decode(reader, reader.uint32());
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.an = AnNetworkEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NetworkEvent {
    return {
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      source: isSet(object.source) ? eventSourceFromJSON(object.source) : 0,
      sequence: isSet(object.sequence) ? globalThis.String(object.sequence) : "0",
      producerInstance: isSet(object.producerInstance)
        ? globalThis.String(object.producerInstance)
        : isSet(object.producer_instance)
        ? globalThis.String(object.producer_instance)
        : "",
      identity: isSet(object.identity) ? MobileIdentity.fromJSON(object.identity) : undefined,
      subscriber: isSet(object.subscriber) ? Subscriber.fromJSON(object.subscriber) : undefined,
      pdsn: isSet(object.pdsn) ? PdsnNetworkEvent.fromJSON(object.pdsn) : undefined,
      bsc: isSet(object.bsc) ? BscNetworkEvent.fromJSON(object.bsc) : undefined,
      msc: isSet(object.msc) ? MscNetworkEvent.fromJSON(object.msc) : undefined,
      pcf: isSet(object.pcf) ? PcfNetworkEvent.fromJSON(object.pcf) : undefined,
      hlr: isSet(object.hlr) ? HlrNetworkEvent.fromJSON(object.hlr) : undefined,
      smsc: isSet(object.smsc) ? SmscNetworkEvent.fromJSON(object.smsc) : undefined,
      bts: isSet(object.bts) ? BtsNetworkEvent.fromJSON(object.bts) : undefined,
      an: isSet(object.an) ? AnNetworkEvent.fromJSON(object.an) : undefined,
    };
  },

  toJSON(message: NetworkEvent): unknown {
    const obj: any = {};
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.source !== 0) {
      obj.source = eventSourceToJSON(message.source);
    }
    if (message.sequence !== "0") {
      obj.sequence = globalThis.String(message.sequence);
    }
    if (message.producerInstance !== "") {
      obj.producerInstance = message.producerInstance;
    }
    if (message.identity !== undefined) {
      obj.identity = MobileIdentity.toJSON(message.identity);
    }
    if (message.subscriber !== undefined) {
      obj.subscriber = Subscriber.toJSON(message.subscriber);
    }
    if (message.pdsn !== undefined) {
      obj.pdsn = PdsnNetworkEvent.toJSON(message.pdsn);
    }
    if (message.bsc !== undefined) {
      obj.bsc = BscNetworkEvent.toJSON(message.bsc);
    }
    if (message.msc !== undefined) {
      obj.msc = MscNetworkEvent.toJSON(message.msc);
    }
    if (message.pcf !== undefined) {
      obj.pcf = PcfNetworkEvent.toJSON(message.pcf);
    }
    if (message.hlr !== undefined) {
      obj.hlr = HlrNetworkEvent.toJSON(message.hlr);
    }
    if (message.smsc !== undefined) {
      obj.smsc = SmscNetworkEvent.toJSON(message.smsc);
    }
    if (message.bts !== undefined) {
      obj.bts = BtsNetworkEvent.toJSON(message.bts);
    }
    if (message.an !== undefined) {
      obj.an = AnNetworkEvent.toJSON(message.an);
    }
    return obj;
  },

  create(base?: DeepPartial<NetworkEvent>): NetworkEvent {
    return NetworkEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<NetworkEvent>): NetworkEvent {
    const message = createBaseNetworkEvent();
    message.timestamp = object.timestamp ?? undefined;
    message.source = object.source ?? 0;
    message.sequence = object.sequence ?? "0";
    message.producerInstance = object.producerInstance ?? "";
    message.identity = (object.identity !== undefined && object.identity !== null)
      ? MobileIdentity.fromPartial(object.identity)
      : undefined;
    message.subscriber = (object.subscriber !== undefined && object.subscriber !== null)
      ? Subscriber.fromPartial(object.subscriber)
      : undefined;
    message.pdsn = (object.pdsn !== undefined && object.pdsn !== null)
      ? PdsnNetworkEvent.fromPartial(object.pdsn)
      : undefined;
    message.bsc = (object.bsc !== undefined && object.bsc !== null)
      ? BscNetworkEvent.fromPartial(object.bsc)
      : undefined;
    message.msc = (object.msc !== undefined && object.msc !== null)
      ? MscNetworkEvent.fromPartial(object.msc)
      : undefined;
    message.pcf = (object.pcf !== undefined && object.pcf !== null)
      ? PcfNetworkEvent.fromPartial(object.pcf)
      : undefined;
    message.hlr = (object.hlr !== undefined && object.hlr !== null)
      ? HlrNetworkEvent.fromPartial(object.hlr)
      : undefined;
    message.smsc = (object.smsc !== undefined && object.smsc !== null)
      ? SmscNetworkEvent.fromPartial(object.smsc)
      : undefined;
    message.bts = (object.bts !== undefined && object.bts !== null)
      ? BtsNetworkEvent.fromPartial(object.bts)
      : undefined;
    message.an = (object.an !== undefined && object.an !== null) ? AnNetworkEvent.fromPartial(object.an) : undefined;
    return message;
  },
};

function createBasePublishRequest(): PublishRequest {
  return { event: undefined };
}

export const PublishRequest: MessageFns<PublishRequest> = {
  encode(message: PublishRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.event !== undefined) {
      NetworkEvent.encode(message.event, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PublishRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePublishRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.event = NetworkEvent.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PublishRequest {
    return { event: isSet(object.event) ? NetworkEvent.fromJSON(object.event) : undefined };
  },

  toJSON(message: PublishRequest): unknown {
    const obj: any = {};
    if (message.event !== undefined) {
      obj.event = NetworkEvent.toJSON(message.event);
    }
    return obj;
  },

  create(base?: DeepPartial<PublishRequest>): PublishRequest {
    return PublishRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PublishRequest>): PublishRequest {
    const message = createBasePublishRequest();
    message.event = (object.event !== undefined && object.event !== null)
      ? NetworkEvent.fromPartial(object.event)
      : undefined;
    return message;
  },
};

function createBasePublishAck(): PublishAck {
  return { assignedSequence: "0" };
}

export const PublishAck: MessageFns<PublishAck> = {
  encode(message: PublishAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.assignedSequence !== "0") {
      writer.uint32(8).uint64(message.assignedSequence);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PublishAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePublishAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.assignedSequence = reader.uint64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PublishAck {
    return {
      assignedSequence: isSet(object.assignedSequence)
        ? globalThis.String(object.assignedSequence)
        : isSet(object.assigned_sequence)
        ? globalThis.String(object.assigned_sequence)
        : "0",
    };
  },

  toJSON(message: PublishAck): unknown {
    const obj: any = {};
    if (message.assignedSequence !== "0") {
      obj.assignedSequence = globalThis.String(message.assignedSequence);
    }
    return obj;
  },

  create(base?: DeepPartial<PublishAck>): PublishAck {
    return PublishAck.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PublishAck>): PublishAck {
    const message = createBasePublishAck();
    message.assignedSequence = object.assignedSequence ?? "0";
    return message;
  },
};

function createBaseListenEventsRequest(): ListenEventsRequest {
  return { sourceFilter: [] };
}

export const ListenEventsRequest: MessageFns<ListenEventsRequest> = {
  encode(message: ListenEventsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.sourceFilter) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListenEventsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListenEventsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.sourceFilter.push(reader.int32() as any);

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.sourceFilter.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListenEventsRequest {
    return {
      sourceFilter: globalThis.Array.isArray(object?.sourceFilter)
        ? object.sourceFilter.map((e: any) => eventSourceFromJSON(e))
        : globalThis.Array.isArray(object?.source_filter)
        ? object.source_filter.map((e: any) => eventSourceFromJSON(e))
        : [],
    };
  },

  toJSON(message: ListenEventsRequest): unknown {
    const obj: any = {};
    if (message.sourceFilter?.length) {
      obj.sourceFilter = message.sourceFilter.map((e) => eventSourceToJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ListenEventsRequest>): ListenEventsRequest {
    return ListenEventsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ListenEventsRequest>): ListenEventsRequest {
    const message = createBaseListenEventsRequest();
    message.sourceFilter = object.sourceFilter?.map((e) => e) || [];
    return message;
  },
};

/**
 * Aggregated event bus. Every component publishes events here as a gRPC
 * client; every consumer subscribes to ListenEvents.
 */
export type EventServiceDefinition = typeof EventServiceDefinition;
export const EventServiceDefinition = {
  name: "EventService",
  fullName: "events.v1.EventService",
  methods: {
    /**
     * Long-lived server-streaming subscription. The server fans out matching
     * events to each subscriber. Slow subscribers are disconnected.
     */
    listenEvents: {
      name: "ListenEvents",
      requestType: ListenEventsRequest as typeof ListenEventsRequest,
      requestStream: false,
      responseType: NetworkEvent as typeof NetworkEvent,
      responseStream: true,
      options: {},
    },
    /**
     * Unary publish. Producers hold a long-lived gRPC channel and rely on
     * HTTP/2 multiplexing — each call is a cheap stream on the existing
     * connection, no per-event TCP/TLS handshake.
     */
    publish: {
      name: "Publish",
      requestType: PublishRequest as typeof PublishRequest,
      requestStream: false,
      responseType: PublishAck as typeof PublishAck,
      responseStream: false,
      options: {},
    },
  },
} as const;

export interface EventServiceImplementation<CallContextExt = {}> {
  /**
   * Long-lived server-streaming subscription. The server fans out matching
   * events to each subscriber. Slow subscribers are disconnected.
   */
  listenEvents(
    request: ListenEventsRequest,
    context: CallContext & CallContextExt,
  ): ServerStreamingMethodResult<DeepPartial<NetworkEvent>>;
  /**
   * Unary publish. Producers hold a long-lived gRPC channel and rely on
   * HTTP/2 multiplexing — each call is a cheap stream on the existing
   * connection, no per-event TCP/TLS handshake.
   */
  publish(request: PublishRequest, context: CallContext & CallContextExt): Promise<DeepPartial<PublishAck>>;
}

export interface EventServiceClient<CallOptionsExt = {}> {
  /**
   * Long-lived server-streaming subscription. The server fans out matching
   * events to each subscriber. Slow subscribers are disconnected.
   */
  listenEvents(
    request: DeepPartial<ListenEventsRequest>,
    options?: CallOptions & CallOptionsExt,
  ): AsyncIterable<NetworkEvent>;
  /**
   * Unary publish. Producers hold a long-lived gRPC channel and rely on
   * HTTP/2 multiplexing — each call is a cheap stream on the existing
   * connection, no per-event TCP/TLS handshake.
   */
  publish(request: DeepPartial<PublishRequest>, options?: CallOptions & CallOptionsExt): Promise<PublishAck>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export type ServerStreamingMethodResult<Response> = { [Symbol.asyncIterator](): AsyncIterator<Response, void> };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
